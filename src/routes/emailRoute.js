import express from 'express';
// import { sendEmail, sendEmailPers, sendFeo } from '../controller/emailController.js';

const router = express.Router();
// router.post('/send', sendEmail);
// router.post('/send-personal', sendEmailPers);
// router.post('/send-feo', sendFeo);

export default router;